# FastFiler 機能アイデアの採否台帳

旧 IDEAS.md (思いつきベースの提案集) を、2026-05-23 のアイデンティティ整理
セッションで採否確定したものに整理し直した台帳。

判断軸は [`/CONTEXT.md`](../CONTEXT.md) の中核アイデンティティ
(B 縦タブ+任意分割ペイン > A 速度 > C Windows 統合 > D 拡張性) に基づく。
個別の重い判断は [`adr/`](./adr/) を参照。

採否ステータス凡例:

- ✅ **採用** — 実装済または採用予定 (現況サマリは [`README.md`](./README.md))
- ❌ **不採用** — 持たない方針 (重い判断は [`adr/`](./adr/))

---

## 即効性が高そうなもの

| # | 名前 | 採否 | 根拠 |
|---|---|---|---|
| 1 | クイックアクセス (お気に入り) サイドバー | ❌ | タブで代替可能 (ユーザー判断) |
| 2 | アドレスバー編集 (`Ctrl+L`) | ✅ | GPUI 版: パスバークリックで編集可 (`Ctrl+L` の割当は未) |
| 3 | ファイル名インクリメンタルサーチ (キー連打先頭一致ジャンプ) | ✅ | floem 版にあったが GPUI 版では**再実装待ち** |
| 4 | ファイル操作の Undo (`Ctrl+Z`) | ✅ | 実装済 (リネーム / ゴミ箱送り。**移動は未配線** — [ADR 0006](./adr/0006-undo-scope.md) 追記) |
| 5 | プログレスダイアログ (大量コピー / 移動) | ✅ | 実装済 (フッター進捗 + キャンセル) |

---

## UI / UX 改善

| # | 名前 | 採否 | 根拠 |
|---|---|---|---|
| 6 | テーマカスタマイズ (アクセントカラー) | ✅ | 実装済 (プリセット + `themes/*.json` カスタムテーマ + スタイル + フォント) |
| 7 | ステータスバー (選択件数 / 合計サイズ / ドライブ空き) | ✅ | 実装済 |
| 8 | キーボードチートシート モーダル | ❌ | 設定ダイアログのホットキータブで代替 (ユーザー判断) |
| 9 | アイコンセット切替 | ❌ | GPUI 版でシステムアイコン (SHGetFileInfo) に置換 — セット切替の需要消滅 |
| 10 | パネルプリセット | ✅ | 採用予定 |

---

## ちょっと尖った機能

| # | 名前 | 採否 | 根拠 |
|---|---|---|---|
| 11 | デュアルパネル同期スクロール | ❌ | ペイン連動と一体で不採用 ([ADR 0001](./adr/0001-remove-pane-linking.md)) |
| 12 | フォルダ比較モード | ❌ | 専用ツールに委ねる (ユーザー判断) |
| 13 | タグ / カラーマーク | ❌ | サイドカーファイル等のリスクを避ける (ユーザー判断) |
| 14 | Quick Look (Spacebar 全画面プレビュー) | ❌ | メディアプレビュー全般を持たない ([ADR 0005](./adr/0005-no-media-preview.md)) |
| 15 | 「最近開いたフォルダ」/ `Ctrl+Shift+T` | ❌ | タブで代替 (ユーザー判断) |
| 16 | ターミナルパネル | ❌ | 内蔵せず、外部ターミナル起動に一本化 ([ADR 0004](./adr/0004-no-builtin-terminal.md)) |
| 17 | Spring-loaded folder (D&D 中のフォルダ自動展開) | ✅ | 採用予定 |
| 18 | プラグインマーケット | ❌ | プラグイン機構ごと不採用 ([ADR 0003](./adr/0003-remove-plugin-system.md)) |

---

## 中身の改善 (技術寄り)

| # | 名前 | 採否 | 根拠 |
|---|---|---|---|
| 19 | USN ジャーナル監視 | ❌ | Everything HTTP API 連携で代替 (本セッションで確定) |
| 20 | ネイティブ IContextMenu 統合 | ✅ | 実装済 — **Shift+右クリック限定** ([ADR 0007](./adr/0007-shell-context-menu-shift-only.md)) |
| 21 | OLE D&D Effect (Move / Copy) 細粒度制御 | ✅ | 実装済 (Ctrl=コピー / Shift=移動 / 右ボタンメニュー) |
| 22 | パフォーマンス計測パネル | ✅ | 採用予定 |
| 23 | ファイルサイズ単位カスタマイズ (KiB / KB) | ❌ | 既定 KiB で固定 (ユーザー判断) |

---

## 飛び道具 (面白枠)

| # | 名前 | 採否 | 根拠 |
|---|---|---|---|
| 24 | ファイルツリーの ASCII エクスポート | ✅ | 採用予定 (domain `ascii_tree.rs` 実装済・UI 未配線) |
| 25 | 夜モード自動 (時刻連動でテーマ切替) | ❌ | 不採用 (本セッションで確定 / 手動切替で十分) |
| 26 | プラグインで音を鳴らす API | ❌ | プラグイン機構ごと不採用 ([ADR 0003](./adr/0003-remove-plugin-system.md)) |
| 27 | 「祈りボタン」(冗談) | ❌ | ネタ枠 |

---

## 新規アイデア追加ルール

このファイルに新しい機能アイデアを追加する場合:

1. 中核アイデンティティ (`/CONTEXT.md`) の優先順位 (B > A > C > D) と
   照らして「やる価値」を見積もる
2. 採否が明らかでないものは「保留」を作らず、`grill-with-docs` セッションを
   1 回挟んで採否を確定させる (保留状態が長引くと「コード温存負債」につながる
   — ADR 0001 の教訓)
3. 採用と決まった項目はこのファイルで ✅ にし、実装に入るときは
   `plan/` に日付付きの計画ファイルを作る
4. 不採用と決まった項目はこのファイルで ❌ にする
   (理由が重いものは `adr/` に記録する)
