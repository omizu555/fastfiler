[workspace]
resolver = "2"
members = [
    "crates/fastfiler-domain",
    "crates/fastfiler-native",
]
exclude = [
    "experimental/floem-filelist-poc",
]

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
