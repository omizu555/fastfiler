{
  "name": "fastfiler-mock",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "縦タブ + 任意分割ペイン + ペイン連動を持つ高速ファイラの UI モック (Vite + Solid)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "solid-js": "^1.8.22"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^5.4.10",
    "vite-plugin-solid": "^2.10.2"
  }
}
