# FastFiler コンテキスト

FastFiler のドメイン共通用語と中核アイデンティティの定義。
実装の詳細や仕様は書かない (それは `doc/` 配下に置く)。

---

## 中核アイデンティティ

FastFiler は **Windows 向けのファイラ**であり、以下の価値を優先度順に提供する。

1. **縦タブ + 任意分割ペイン** (最重要)
   多数のフォルダを同時に開き、行き来する作業効率。
   FastFiler の存在意義の中心。
2. **速度**
   大量ファイル (例: `C:\Windows\System32`) でも瞬時に開け、操作がもたつかない。
   1 を快適に使うための前提条件。
3. **Windows との深い統合**
   シェル拡張 / OLE D&D / 既定ハンドラなど、エクスプローラの置き換えとして機能すること。
   1 と 2 を補強するための手段。
4. **拡張性**
   プラグイン / ホットキー / テーマで自分仕様に育てられること。
   余力で提供する付加価値。

この優先順位は機能取捨選択の判断軸となる。たとえば「クイックアクセス (お気に入り)」が
タブで代替可能なら採用しない、といった判断はこの軸から導かれる。

---

## 用語

### タブ (Tab)
縦タブパネルに並ぶ単位。それぞれが独自の **ペイン分割構成 (BSP ツリー)** と
表示中フォルダ・選択状態・履歴を保持する独立した作業文脈。
タブ名は primary pane の表示中フォルダ名に追従する。

### ペイン (Pane)
タブの中で実際にファイル一覧 (またはペイン内ツリー) を表示する矩形領域。
1 つのタブは 1 つ以上のペインを持ち、任意の縦/横分割でネスト可能 (BSP)。

### フォーカスペイン (Focus pane)
直近で操作されたペイン。ホットキーやワークスペースツリーのクリックなど、
「どのペインに作用するか」が曖昧な操作の宛先となる唯一のペイン。
タブを切り替えても、そのタブで前回フォーカスしていたペインが復元される。
**FastFiler では「アクティブな宛先」はフォーカスペイン 1 つだけ** (連動 Red/Blue は廃止: ADR 0001)。

### ワークスペースツリー (Workspace tree)
ドライブ起点のグローバルなツリーパネル。タブ・ペインとは独立して常に表示可能で、
ノードをクリックすると **フォーカスペイン** にそのフォルダを開く。
ローカルドライブと **サーバノード** がルートに並ぶ。

### サーバノード (Server node)
ワークスペースツリーのルートに置かれる、UNC サーバ (`\\server`) を表す仮想ノード。
**実在パスではなく** 配下に登録された share をグルーピングするためだけのコンテナで、
クリックしてもナビゲートはしない (子の **share ノード** をクリックする)。
ペインで UNC を開いた瞬間に自動登録される (右クリックで「サーバごと削除」可)。

### share ノード (Share node)
サーバノード配下に置かれる `\\server\share` を表す通常のツリーノード。
クリックすると share ルートをペインに開く。`settings.ron` の `tree_unc_shares`
に永続化され、次回起動時にサーバが応答しなくてもツリー UI は壊れない。

### ペイン内ツリー (In-pane tree)
ペインの表示モードのひとつ。リスト表示と切替で、そのペインの表示中フォルダを
起点とした子ツリーを表示する。ワークスペースツリーとは別概念
(起点が異なる: ワークスペースツリー = ドライブ起点 / ペイン内ツリー = ペイン cwd 起点)。
**フォルダのみを表示し、ファイルは含めない** (ワークスペースツリーと操作モデルを統一)。

### ドックスロット (Dock slot)
パネル (タブパネル / ワークスペースツリーパネル / 将来のターミナル等) を
配置できる位置。`left / right / top / bottom / hidden` の 5 ヶ所が存在し、
**各スロットには常に高々 1 パネルだけ** が入る (ADR 0002)。

### ドロップゾーン (Drop zone)
外部アプリ (エクスプローラ等) からの D&D 受信時、宛先を判定するための単位。
FastFiler では **ペインの矩形領域 1 つを 1 ゾーン** とし、フォルダ行や個別 UI 部品
単位の細分はしない (ADR 0009)。ゾーン内にドロップされたファイルは、そのペインの
**現在表示中のフォルダ** に対して Move/Copy される。
内部 D&D (FastFiler 内部のペイン間) はフォルダ行への直接ドロップも扱うため、
この用語は外部 D&D 受信時のみ用いる。

### 右ボタン D&D (Right-button drag & drop)
マウスの右ボタンを押しながらドラッグし、ドロップ時にコンテキストメニュー
(`ここにコピー / ここに移動 / キャンセル`) を出して動作を選ばせる方式。
左ボタン D&D が修飾キー (Ctrl/Shift) で effect を確定させるのに対し、
右ボタン D&D は **ドロップ後にユーザが選ぶまで effect 未確定**。
適用範囲は FastFiler 内部のペイン間 D&D と、外部 (Explorer 等) からの受信。
外部送信 (FastFiler → Explorer) は今後の拡張対象 (ADR 0010)。

### 拡張点 (Extension points)
FastFiler 自体に対するユーザーカスタマイズの入口。以下の 3 軸に限定される (ADR 0003):

1. **ユーザーコマンド** (`%APPDATA%\FastFiler\commands\commands.json`)
   — 右クリックメニューから任意の外部コマンドを実行
2. **シェル統合** (将来) — Windows の IContextMenu を経由したシェル拡張呼び出し
3. **テーマ / ホットキー / アイコンセット / フォント** — UI/UX カスタマイズ

アプリ内 JavaScript/WASM プラグイン機構は持たない。
